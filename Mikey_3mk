local screenGui = Instance.new("ScreenGui") screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui") local textBox = Instance.new("TextLabel") textBox.Parent = screenGui textBox.Size = UDim2.new(0, 200, 0, 50) textBox.Position = UDim2.new(0.5, -100, 0.5, -25) textBox.Text = "الحذف تم" textBox.TextColor3 = Color3.fromRGB(math.random(0, 255), math.random(0, 255), math.random(0, 255)) textBox.TextSize = 40 textBox.BackgroundTransparency = 1 textBox.TextEditable = false
